/**
 * Request DTOs — data transfer objects for incoming API payloads.
 *
 * Annotated with Bean Validation constraints (@NotBlank, @Valid, etc.)
 * so invalid input is rejected before the service layer is reached.
 */
package com.psl.oms.dto.request;
