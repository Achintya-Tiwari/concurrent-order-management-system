/**
 * Controller layer — REST endpoint definitions.
 *
 * Contains:
 *   - CustomerController   — CRUD for /api/customers
 *   - OrderController      — order lifecycle, billing, and batch operations at /api/orders
 *   - StockItemController  — product catalogue at /api/stocks
 *   - ReportController     — analytics at /api/reports
 */
package com.psl.oms.controller;
