/**
 * Service layer — business logic and transaction management.
 *
 * Contains:
 *   - CustomerService    — customer registration and lookup
 *   - OrderService       — transactional order placement, shipping, billing
 *   - StockItemService   — product catalogue and inventory
 *   - ReportingService   — monthly analytics and top-customer queries
 */
package com.psl.oms.service;
